version: 2

models:
  - name: products_t
    columns:
      - name: price
        tests:
          - dbt_utils.expression_is_true:
              arguments:
                expression: ">= 0"

  - name: orders_t
    columns:
      - name: order_id
        
        data_tests: 
          - not_null
          - unique